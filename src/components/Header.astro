---
import { getRoutes, useTranslations, getLangFromUrl } from '../i18n/utils';
import LangSwitcher from './LangSwitcher.astro';
import { site } from '../data/site';
import reglages from '../data/reglages.json';

interface Props { lang?: 'fr' | 'en'; transparent?: boolean; }
const lang = Astro.props.lang ?? getLangFromUrl(Astro.url);
const { transparent = false } = Astro.props;
const t = useTranslations(lang);
const r = getRoutes(lang);

const path = Astro.url.pathname.replace(/\/+$/, '') || '/';
const norm = (h: string) => h.replace(/\/+$/, '') || '/';
const isActive = (href: string) => {
  const h = norm(href);
  if (h === norm(r.home)) return path === h;
  return path === h || path.startsWith(h + '/');
};

const links = [
  { href: r.home, label: t('nav.home') },
  { href: r.projects, label: t('nav.projects') },
  ...(reglages.afficherFormation ? [{ href: r.training, label: t('nav.training') }] : []),
  { href: r.about, label: t('nav.about') },
  { href: r.contact, label: t('nav.contact') },
];
---
<header class:list={["site-header", { transparent }]}>
  <div class="header-inner container-wide">
    <a href={r.home} class="brand" aria-label={`${site.name} — accueil`}>
      <span class="brand-name">Kévin Richarte</span>
    </a>

    <nav class="nav-desktop" aria-label="Navigation principale">
      <ul role="list">
        {links.map((l) => (
          <li>
            <a
              href={l.href}
              class:list={["nav-link", { active: isActive(l.href) }]}
              aria-current={isActive(l.href) ? 'page' : undefined}
            >{l.label}</a>
          </li>
        ))}
      </ul>
    </nav>

    <div class="header-right">
      <div class="header-lang"><LangSwitcher lang={lang} /></div>
      <button class="burger" aria-label={t('nav.menu')} aria-expanded="false" aria-controls="mobile-menu">
        <span></span><span></span><span></span>
      </button>
    </div>
  </div>

  <div class="mobile-menu" id="mobile-menu" hidden>
    <nav aria-label="Navigation">
      <ul role="list">
        {links.map((l) => (
          <li>
            <a href={l.href} class:list={["m-link", { active: isActive(l.href) }]}
               aria-current={isActive(l.href) ? 'page' : undefined}>{l.label}</a>
          </li>
        ))}
      </ul>
    </nav>
    <div class="mobile-lang"><LangSwitcher lang={lang} /></div>
  </div>
</header>

<style>
  .site-header {
    position: fixed; inset: 0 0 auto 0; height: var(--header-h);
    z-index: var(--z-header);
    display: flex; align-items: center;
    border-bottom: 1px solid transparent;
    transition: background var(--dur) var(--ease), border-color var(--dur) var(--ease), backdrop-filter var(--dur) var(--ease), transform 250ms var(--ease);
  }
  /* Non-transparent (pages intérieures) : fond dès le départ */
  .site-header:not(.transparent) {
    background: var(--encre-92);
    -webkit-backdrop-filter: blur(10px);
    backdrop-filter: blur(10px);
    border-bottom-color: var(--line);
  }
  /* Transparent (accueil) : devient solide au scroll */
  .site-header.transparent.is-solid {
    background: var(--encre-92);
    -webkit-backdrop-filter: blur(10px);
    backdrop-filter: blur(10px);
    border-bottom-color: var(--line);
  }

  .header-inner { display: flex; align-items: center; justify-content: space-between; gap: 1.5rem; width: 100%; }

  .brand { display: inline-flex; align-items: baseline; }
  .brand-name { font-family: var(--font-serif); font-size: 1.35rem; line-height: 1; color: var(--ivoire); letter-spacing: 0.01em; }

  .nav-desktop ul { display: flex; gap: 1.9rem; }
  .nav-link {
    font-family: var(--font-sans); font-size: 0.78rem; letter-spacing: 0.11em; text-transform: uppercase;
    color: var(--ivoire-72); padding-block: 0.4rem; position: relative; transition: color var(--dur) var(--ease);
  }
  .nav-link:hover { color: var(--ivoire); }
  .nav-link.active { color: var(--ivoire); }
  .nav-link.active::after {
    content: ""; position: absolute; left: 0; right: 0; bottom: -3px; height: 1px; background: var(--ambre);
  }

  .header-right { display: flex; align-items: center; gap: 1.25rem; }

  .burger { display: none; width: 30px; height: 22px; position: relative; }
  .burger span { position: absolute; left: 0; right: 0; height: 1.5px; background: var(--ivoire); transition: transform var(--dur) var(--ease), opacity var(--dur) var(--ease); }
  .burger span:nth-child(1) { top: 2px; }
  .burger span:nth-child(2) { top: 10px; }
  .burger span:nth-child(3) { top: 18px; }
  .burger[aria-expanded="true"] span:nth-child(1) { transform: translateY(8px) rotate(45deg); }
  .burger[aria-expanded="true"] span:nth-child(2) { opacity: 0; }
  .burger[aria-expanded="true"] span:nth-child(3) { transform: translateY(-8px) rotate(-45deg); }

  .mobile-menu {
    position: fixed; inset: var(--header-h) 0 0 0; z-index: calc(var(--z-header) - 1);
    background: var(--encre); border-top: 1px solid var(--line);
    display: flex; flex-direction: column; justify-content: space-between;
    padding: 2.5rem var(--gutter) 3rem;
  }
  .mobile-menu[hidden] { display: none; }
  .mobile-menu ul { display: flex; flex-direction: column; gap: 0.4rem; }
  .m-link {
    font-family: var(--font-serif); font-size: clamp(2rem, 9vw, 2.8rem); font-weight: 300; color: var(--ivoire-72);
    display: inline-block; padding-block: 0.35rem;
  }
  .m-link.active { color: var(--ambre); }
  .mobile-lang { padding-top: 2rem; }

  @media (max-width: 820px) {
    .nav-desktop, .header-lang { display: none; }
    .burger { display: block; }
  }

  /* Nav escamotable (mobile ≤899px) : masquée en descendant, réapparaît en remontant — le bureau ne la déclenche jamais */
  @media (max-width: 899px) {
    .site-header.is-hidden { transform: translateY(-100%); }
  }

  /* Paysage mobile : nav compacte (menu ☰) pour ne pas couvrir la vidéo — nom à gauche, menu à droite */
  @media (max-width: 899px) and (orientation: landscape) {
    .nav-desktop, .header-lang { display: none; }
    .burger { display: block; }
    /* Nom réduit pour empiéter moins sur la vidéo */
    .brand-name { font-size: 1rem; }
    /* Menu = panneau à droite (le showreel reste visible à gauche) ; liens compacts + défilables */
    .mobile-menu {
      left: auto; width: max-content; max-width: 60%;
      justify-content: flex-start; align-items: flex-end; gap: 0.3rem;
      overflow-y: auto; padding-block: 1.1rem;
      border-left: 1px solid var(--line);
    }
    .mobile-menu ul { align-items: flex-end; }
    .m-link { font-size: 1.5rem; padding-block: 0.25rem; text-align: right; }
    .mobile-lang { padding-top: 1rem; align-self: flex-end; }
  }

  /* Portrait mobile : menu = liens en LIGNE horizontale dans l'espace au-dessus du showreel (qui reste visible) */
  @media (max-width: 899px) and (orientation: portrait) {
    .mobile-menu {
      inset: var(--header-h) 0 auto 0;          /* juste sous l'en-tête, hauteur selon le contenu */
      background: transparent; border-top: none;
      justify-content: flex-start; align-items: center;
      gap: 0.2rem; padding: 0.4rem var(--gutter) 0.45rem;
    }
    .mobile-menu ul { flex-direction: row; flex-wrap: wrap; justify-content: center; gap: 0.35rem 1.3rem; }
    .m-link { font-size: 1.02rem; padding-block: 0.05rem; }
    .mobile-lang { padding-top: 0.2rem; }
  }
  :global(body.menu-open) { overflow: hidden; }
</style>

<script>
  const header = document.querySelector('.site-header');
  const isMobile = () => matchMedia('(max-width: 899px)').matches;
  const HIDE_THRESHOLD = 80;   // ne pas masquer avant d'avoir descendu ce seuil
  const JITTER = 10;           // ignorer les micro-scrolls
  let lastY = window.scrollY;
  const onScroll = () => {
    if (!header) return;
    const y = window.scrollY;
    // Fond solide (accueil transparent) au-delà de 24px — comportement existant conservé
    if (y > 24) header.classList.add('is-solid');
    else header.classList.remove('is-solid');
    // Escamotage — mobile uniquement, jamais sur ordinateur
    if (!isMobile()) { header.classList.remove('is-hidden'); lastY = y; return; }
    if (y <= 4) { header.classList.remove('is-hidden'); lastY = y; return; }        // haut de page : toujours visible
    if (document.body.classList.contains('menu-open')) return;                      // menu ouvert : on ne masque pas
    const delta = y - lastY;
    if (Math.abs(delta) < JITTER) return;                                           // micro-scroll ignoré (anti-tremblement)
    if (delta > 0 && y > HIDE_THRESHOLD) header.classList.add('is-hidden');         // on descend → masquer
    else header.classList.remove('is-hidden');                                      // on remonte → réapparaître
    lastY = y;
  };
  onScroll();
  window.addEventListener('scroll', onScroll, { passive: true });

  const burger = document.querySelector('.burger');
  const menu = document.getElementById('mobile-menu');
  const setOpen = (open: boolean) => {
    if (!burger || !menu) return;
    burger.setAttribute('aria-expanded', String(open));
    menu.hidden = !open;
    document.body.classList.toggle('menu-open', open);
  };
  burger?.addEventListener('click', () => {
    setOpen(burger.getAttribute('aria-expanded') !== 'true');
  });
  menu?.querySelectorAll('a').forEach((a) => a.addEventListener('click', () => setOpen(false)));
  window.addEventListener('keydown', (e) => { if (e.key === 'Escape') setOpen(false); });
</script>
